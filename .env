# Bingopedia Revamp – Environment Variables Template
#
# Copy this file to .env.local and fill in your actual values.
# The .env.local file is gitignored and will not be committed.
#
# For detailed documentation, see ENVIRONMENT_AND_CONFIG.md

# MongoDB Atlas Configuration (Backend Only)
# Required for both local Express server and Vercel serverless functions
MONGODB_USERNAME=untitledspacecraftcontrollers_db_user
MONGODB_PASSWORD=tcn4yrc8bgq-DXB0vqe
MONGODB_CLUSTER=cluster0.rvkwijm.mongodb.net

# Local Express Server Port (Backend Only)
# Only used by the local dev server (server/index.ts)
# Vercel serverless functions ignore this
PORT=3001

# Frontend API Base URL (Optional)
# If unset, the frontend will use same-origin /api/leaderboard
# Only set this if you want to point the frontend at a separate local API
# VITE_API_URL=http://localhost:3001
